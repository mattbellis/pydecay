'''
This module exists exclusively so that Django can have the
directory structure it expects: manage.py and settings.py in one
directory, and the models in a subdirectory (this one) whose name
will be prefixed to table names.
'''